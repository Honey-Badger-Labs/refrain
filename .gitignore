node_modules/
dist/
build/
*.tsbuildinfo
.DS_Store
.env
.env.local

# Pipeline working area: candidates are unpublished audio and must never be
# committed or served (SEC-6).
work/candidates/
work/tmp/
work/**/*.wav

# The studio's local store holds reviewer verdicts and feedback (SEC-5).
data/store.local.json

# Published output is generated by `refrain publish`; the pilot output under
# apps/listener/public/library is committed on purpose so GitHub Pages can
# serve it without a render run.
coverage/
playwright-report/
test-results/
smoke-shots/
