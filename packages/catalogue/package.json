{
  "name": "@refrain/catalogue",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The contract between the studio and the listener app: record schemas, catalogue building, route parsing and integrity hashing.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
